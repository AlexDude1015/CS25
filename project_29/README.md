Class Assignment 29

---

This program parses a specific string into a movie structure's
members, "title" and "release year." 

Each object's members are private. Therefore, they can only be accessed 
by member functions. To hide the complexity, a operation overload was created, so users
can extract objects to streams easily.
