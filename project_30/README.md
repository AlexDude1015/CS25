Class assignment 30

---

This program has a class called Text_box with two
constructors. The default constructor doesn't accept 
arguments, but the second one accepts a string argument. 
Furthermore, it's an explicit constructor.